The invite screenshots in this onboarding guide are stale. They still show the old
workspace switcher, which was removed last quarter. If a customer hits the old invite
flow, create an engineering issue and link this document. Do not send the customer a
workaround that bypasses the admin role check.

A correct onboarding path is: admin sends an invite, the teammate accepts from the
same email domain, and the first login uses the workspace the admin selected. Support
owns copy fixes. Engineering owns the screenshot refresh and the broken deep link.
